
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing postman tests",
  "directories":{
	  "test":"tests"
  },

  "scripts": {
    "api-test-test": "newman run tests/AddClient.e2e.json --insecure"
  },
  "author": "Victor I p.",
  "dependencies": {
    "newman": "^4.5.1"
  }
}
