{  
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing postman tests",
  "directories":{
	  "test":"tests"
  },

  "scripts": {
    "api-test": "newman run tests/*.json --insecure --reporters cli,junit --reporter-junit-export newman.xml",
     "api-test-delete": "newman run tests/DeleteClient.json --insecure --reporters cli,junit --reporter-junit-export newman.xml"	  
  },
  "author": "Victor I p.",
  "dependencies": {
    "newman": "^4.5.1"
  }
}
